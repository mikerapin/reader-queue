import React from 'react';
import './NextInQueue.scss';

const NextInQueue = (): React.ReactElement => {
  const cool = 'cool';
  return <div className="next-in-queue">${cool}</div>;
};

export default NextInQueue;
